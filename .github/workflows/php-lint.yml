on: [push, pull_request]
name: Main
jobs:
  php-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: overtrue/phplint@master
        with:
          path: .
          options: --exclude=*.log
          

